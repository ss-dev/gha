name: Build p1 pipeline

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - "master"
    paths:
      - "src/pipelines/pipeline1.yaml"

jobs:
  build:
    uses: ./.github/workflows/build_pipeline.yml
    with:
      pipeline_name: "p1"
